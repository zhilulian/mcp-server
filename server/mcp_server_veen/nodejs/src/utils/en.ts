import { ILocaleStrings } from './interfaces';

/**
 * Locale strings.
 */
export const strings: ILocaleStrings = {
  resultInJson: 'The result is in following JSON.',
  reasonFollowing: 'The reason of error is following.',
  failAccessRes: 'Failed to access the resouce.',
  failAction: 'Failed to process the action.',
  noResponse: 'No response.',
};
